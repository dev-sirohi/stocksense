# ── Web framework ──────────────────────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1         # ASGI server with WebSocket and HTTP/2 support

# ── Database (async) ───────────────────────────────────────────────────────────
sqlalchemy==2.0.36                # ORM + query builder (async support via 2.0 API)
asyncpg==0.30.0                   # async PostgreSQL driver (replaces psycopg2 for async)
#psycopg2-binary==2.9.10           # sync driver — still needed by Alembic
alembic==1.14.0                   # database migrations
pgvector==0.3.6                   # pgvector SQLAlchemy integration for Vector columns

# ── Caching ────────────────────────────────────────────────────────────────────
redis==5.2.1                      # redis.asyncio is included — the async Redis client

# ── AI / OpenAI ────────────────────────────────────────────────────────────────
openai==1.57.4                    # AsyncOpenAI client for embeddings + chat completions

# ── Utilities ──────────────────────────────────────────────────────────────────
python-dotenv==1.0.1              # loads .env file into os.environ
faker==33.1.0                     # generates realistic fake data for seeding
httpx==0.28.1                     # async HTTP client (used internally by openai SDK)
