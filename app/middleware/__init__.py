# Python note: An empty __init__.py marks this directory as a Python package.
# C# equivalent: there is none — C# uses namespaces declared in each file.
# In Python, without this file, 'from app.middleware.performance import ...' fails.
